fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pyyaml>=6.0
pydantic>=2.7

# Optional. Semantic retrieval works better with these, and degrades to
# lexical-only scoring when they are missing (see jobkb/embed.py).
#   model2vec>=0.3
#   numpy>=1.26
